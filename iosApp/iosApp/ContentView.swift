import SwiftUI

/// Placeholder de Fase 1 -- sin UI real todavía (Compose Multiplatform para iOS llega en la Fase 7).
struct ContentView: View {
    var body: some View {
        Text("Acopio -- Fase 1: núcleo sin UI todavía")
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
